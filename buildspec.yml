version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
   commands:
     build:
    commands:
      - echo Entered the Json code
      - echo Build started on `date`
      - mvn install
      - npm run test
      - package.json
      - basic_server 
      - user.js
      - package-lock.json
      
